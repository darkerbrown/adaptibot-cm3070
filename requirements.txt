absl-py==2.3.0
cleanrl==1.2.0
gymnasium==1.2.0
mujoco==3.3.5
numpy==2.2.6
opencv-python==4.11.0.86
pandas==2.3.0
Pillow==11.3.0
pytest==8.4.2
scipy
seaborn==0.13.2
tensorboard==2.19.0
tensorboardX==2.6.4
torch==2.5.1+cu121
torchaudio==2.5.1+cu121
torchvision==0.20.1
transformers==4.44.2
huggingface-hub==0.34.4

# Note: this pinned file contains packages observed in the local dev environment.
# Some packages (mujoco) require system binaries / licenses. If you plan to
# reproduce this environment on a different machine, prefer creating a
# virtualenv and running:
#   python -m pip install -r requirements.txt
# or export a full freeze with `pip freeze > requirements-locked.txt`.
numpy>=1.24
torch>=2.0
gymnasium
mujoco
matplotlib
pandas
seaborn
# Core runtime
numpy
torch
gymnasium
mujoco

# Perception / vision
torchvision
transformers
opencv-python
Pillow

# Analysis & plotting
matplotlib
seaborn
pandas
scipy

# Utilities / experiment tracking
tensorboard
tensorboardX

# Testing / development
pytest
mypy

# Optional (system dependent)
# - Install mujoco binaries and license separately. The 'mujoco' Python package
#   is required for local MuJoCo runs but CI may skip heavy smoke-runs.
